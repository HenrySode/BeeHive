# Backend

Not yet scaffolded. This will be the Node.js REST API described in `architecture.md`, `api-spec.md`, and `schema.md`, built with TypeScript and Prisma against PostgreSQL.

Git does not track empty directories, which is why this folder did not appear in the repository until this file was added. See the root `README.md` for the current project status and `prd.md` for scope.
